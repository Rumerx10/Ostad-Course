export const createTask = (req, res) => {
  return res.json({ message: "Create task is completed." });
};
export const readTask = (req, res) => {
  return res.json({ message: "Read task is completed." });
};
export const updateTask = (req, res) => {
  return res.json({ message: "Update task is completed." });
};
export const deleteTask = (req, res) => {
  return res.json({ message: "Delete task is completed" });
};
